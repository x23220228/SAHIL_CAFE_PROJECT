version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11

  pre_build:
    commands:
      - python -m pip install --upgrade pip
      - pip install -r requirements.txt

  build:
    commands:
      - chmod +x sonar_scan.sh
      - ./sonar_scan.sh
